spring:
  application:
    name: search-service
  profiles:
    active: dev
  cloud:
    nacos:
      server-addr: xxx
      config:
        file-extension: yaml
        shared-configs:
          - dataId: shared-jdbc.yaml
          - dataId: shared-log.yaml
          - dataId: shared-swagger.yaml
          - dataId: shared-mq.yaml
